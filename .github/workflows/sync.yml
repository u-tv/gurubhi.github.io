name: Daily Dailymotion Auto-Sync

on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  sync-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: npm

      - name: Install Dependencies
        run: npm ci

      - name: Fetch Dailymotion Videos
        run: |
          node <<'NODE'
          const fs = require('fs');

          const user = 'dhamtan';
          const url = `https://api.dailymotion.com/user/${user}/videos?fields=id,title,thumbnail_url&limit=100`;

          async function fetchAll() {
            let allVideos = [];
            for (let page = 1; page <= 5; page++) {
              const res = await fetch(`${url}&page=${page}`);
              const data = await res.json();
              if (data.list && data.list.length > 0) {
                allVideos = allVideos.concat(data.list);
              } else {
                break;
              }
            }
            fs.mkdirSync('src/data', { recursive: true });
            fs.writeFileSync('src/data/videos.json', JSON.stringify(allVideos, null, 2));
            console.log(`Synced ${allVideos.length} videos`);
          }

          fetchAll().catch(err => {
            console.error(err);
            process.exit(1);
          });
          NODE

      - name: Build Astro Site
        run: npm run build

      - name: Commit Updated Data
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          git add src/data/videos.json
          git diff --cached --quiet && exit 0
          git commit -m "Automated Sync: update videos [skip ci]"
          git push

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages Artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./dist

      - name: Deploy to GitHub Pages
        uses: actions/deploy-pages@v4
